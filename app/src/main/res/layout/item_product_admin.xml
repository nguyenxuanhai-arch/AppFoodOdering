<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/bg_food_card"
    android:layout_marginBottom="12dp"
    android:padding="12dp"
    android:elevation="2dp">

    <!-- Hình món ăn -->
    <ImageView
        android:id="@+id/imgAdminProduct"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:scaleType="centerCrop"
        android:src="@drawable/sample_food"
        android:contentDescription="Ảnh món ăn" />

    <!-- Thông tin món ăn -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp">

        <TextView
            android:id="@+id/tvAdminProductName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tên món ăn"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="?android:textColorPrimary" />

        <TextView
            android:id="@+id/tvAdminProductPrice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="7.000đ"
            android:textColor="@color/md_theme_light_primary"
            android:textStyle="bold"
            android:textSize="16sp" />
    </LinearLayout>

    <!-- Nút menu cho admin -->
    <ImageView
        android:id="@+id/btnMenu"
        android:layout_width="36dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:contentDescription="Menu sản phẩm"
        android:padding="8dp"
        android:src="@drawable/ic_more_vert"
        />

</LinearLayout>
